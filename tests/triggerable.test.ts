import { Triggerable, Button } from "../controller_lib/types/triggerable";

// const triggerFn = (self, val) => {console.log("trigger fn", self, val); return val};

// const handleTrigger = () =>

// test("Construct triggerable", ()  =>
// {
// 	const ttrigger_consttruct = new Triggerable(triggerFn, )
// })
